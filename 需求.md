# 背景

ShopX 是由AI驱动基于 JSON Schema 配置化生成电商网站项目。

## 相关项目

ShopX - 基于 JSON Schema 的渲染层
ShopX-UI - 提供 ShopX 组件及变体，例如 Header, Hero, Product Card, Cart, Footer等，每个组件只接收2个PROPS，config 用于组件配置 & data 提供组件所需数据
ShopX-Preview - 提供页面的编辑功能，提供给Seller使用，可视化编辑页面上的一些元素，比如 title, subtitle, background, color等

## 问题

以上项目为3个独立项目，我的问题是在Shopx-Preview项目里如何在用户点击可编辑元素完成修改后能够对应更新到全局的 Json Config 配置中？我的卡点是如果碰到了嵌套且数组类型的情况，如何处理？
